<?xml version="1.0"?>
<launch>
  <!-- This launch file is auto-generated by the autoware_system_designer -->
  <!-- Deployment variant wrapper -->
  <!-- Deploy: {{ deploy_name }} / Mode: {{ mode_key }} / Compute Unit: {{ compute_unit }} -->

{% for variable in variables %}
  <arg name="{{ variable.name }}" default="{{ variable.value }}"/>
{% endfor %}

  <include file="$(dirname)/{{ base_launcher_path }}">
{% for variable in variables %}
    <arg name="{{ variable.name }}" value="$(var {{ variable.name }})"/>
{% endfor %}
  </include>
</launch>
